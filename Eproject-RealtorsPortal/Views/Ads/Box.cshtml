@{
    Layout = null;
}
@model Eproject_RealtorsPortal.Models.Product
<div class="col mb-5">
    <div class="card h-100">
        <!-- Product image-->
        <img class="card-img-top" src="@Model.ProductImage" alt="..." />
        <!-- Product details-->
        <div class="card-body p-4">
            <div class="text-center">
                <!-- Product name-->
                <h5 class="fw-bolder">@Model.ProductTitle</h5>
                <!-- Product price-->
@*                @if (@Model.GetBusinessTypeID(Model) == 1)
*@                    @string.Format("{0:n}", @Model.ProductPrice) / tháng
@*                @if (@Model.GetBusinessTypeID(Model) == 2)
*@                    @string.Format("{0:n}", @Model.ProductPrice)|@Model.ProductArea / m &sup2;
                </div>
        </div>
            <!-- Product actions-->
            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                <div class="text-center"><a class="btn btn-outline-dark mt-auto" href="@Url.Action("Detail", "Product", new {id = Model.ProductId})">View details</a></div>
            </div>
    </div>
</div>